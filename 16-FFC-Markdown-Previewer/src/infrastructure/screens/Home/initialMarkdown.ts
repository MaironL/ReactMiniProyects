export const initialMarkdown = `
# Welcome to my React Markdown Previewer! This is H1

## This is a sub-heading (h2)...

this is a link There's also [links](https://es.reactjs.org/) to **react**

This is a JSX element, \`const element = <h1>Hello, world!</h1>;\`, between 2 backticks.

\`\`\`
// this is multi-line code:
function getGreeting(user) {
  if (user) {
    return <h1>Hello, {formatName(user)}!</h1>;
  }
  return <h1>Hello, Stranger.</h1>;
}
\`\`\`

> Block Quotes!

1. And there are numbered lists too.
1. Use just 1s if you want!
1. And last but not least, let's not forget embedded images:

![Just a few pinguins](https://cdn.pixabay.com/photo/2018/07/28/02/26/puffins-3567175_960_720.jpg)

`;
